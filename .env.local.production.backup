# 🚀 LOCAL DEVELOPMENT ENVIRONMENT - ISOLATED FROM PRODUCTION
# This environment is completely separate from production Vercel/Supabase

# === DATABASE CONFIGURATION ===
# Local SQLite database - NO CONNECTION TO PRODUCTION SUPABASE
DATABASE_URL="file:./prisma/dev_local.db"

# === AUTHENTICATION CONFIGURATION ===
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="local-dev-secret-key-not-for-production-use-only"

# === LOCAL DEVELOPMENT FLAGS ===
NODE_ENV="development"
ENVIRONMENT="local"

# === FEATURE FLAGS ===
ENABLE_LOCAL_DEV_MODE="true"
DISABLE_EXTERNAL_APIS="true"

# === LOCAL PORTS ===
PORT=3000
PRISMA_STUDIO_PORT=5555

# === SECURITY SETTINGS ===
# These are for local development only - never use in production
LOCAL_DEV_MODE="true"
SKIP_ENV_VALIDATION="false"

# === LOGGING ===
DEBUG="prisma:*"
LOG_LEVEL="debug"

# Note: No production API keys, tokens, or external service credentials
# This ensures complete isolation from production services
